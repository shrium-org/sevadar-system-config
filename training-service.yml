server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/training_service_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
